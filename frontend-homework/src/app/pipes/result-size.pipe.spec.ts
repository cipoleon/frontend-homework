import { ResultSizePipe } from './result-size.pipe';

describe('ResultSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ResultSizePipe();
    expect(pipe).toBeTruthy();
  });
});
